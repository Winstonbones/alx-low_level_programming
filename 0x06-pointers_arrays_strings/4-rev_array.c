#include "stdio.h>"
/**
 * _array -  a function that reverses the content of an array of integers
 * @a: string value
 * @n: string value
 *
 * Return: void
 */
void reverse_array(int *a, int n)

{
	
	int p;

	int p = 0

	for ( p < n , p++)
	{

		int a[p] = int n[p]
		int a[p] = i
		int n[p] = i
	}
}

