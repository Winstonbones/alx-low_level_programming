#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    /* Display text */
    printf("With proper grammar, but the outcome is a piece of art,\n");
    return (0);
}
